<section class="h-full rounded-2xl border border-white/10 bg-white/[0.04] p-5"><p class="text-xs font-black uppercase tracking-[0.16em] text-amber-400">Agenda</p><h2 class="mt-1 text-lg font-black">{{ $widget['name'] }}</h2>@if($data->isEmpty())<p class="mt-5 text-sm text-stone-400">Nenhuma conta em aberto.</p>@else<ul class="mt-5 space-y-3">@foreach($data as $row)<li class="flex gap-3 border-b border-white/5 pb-3"><div class="min-w-14 text-xs font-black {{ $row->due_date->isPast() ? 'text-red-300' : ($row->due_date->isToday() ? 'text-amber-300' : 'text-stone-400') }}">{{ $row->due_date->isToday() ? 'Hoje' : ($row->due_date->isTomorrow() ? 'Amanhã' : $row->due_date->format('d/m')) }}</div><div class="min-w-0 flex-1"><p class="truncate font-semibold">{{ $row->supplier?->name ?? $row->description }}</p><p class="text-xs text-stone-400">R$ {{ \App\Support\DecimalFormatter::format((string) $row->outstanding_amount, 2) }}</p></div></li>@endforeach</ul>@endif</section>
