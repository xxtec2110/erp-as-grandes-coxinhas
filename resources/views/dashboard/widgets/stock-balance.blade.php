<section class="rounded-2xl border border-white/10 bg-white/[0.04] p-5 sm:p-6"><div><p class="text-xs font-black uppercase tracking-[0.16em] text-amber-400">Estoque</p><h2 class="mt-1 text-xl font-black">{{ $widget['name'] }}</h2></div>
    @if($data->isEmpty())<p class="mt-5 text-sm text-stone-400">Nenhum produto ativo cadastrado.</p>@else<div class="mt-5 overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="text-xs uppercase text-stone-500"><tr><th class="pb-3">Produto</th><th class="pb-3">Saldo</th><th class="pb-3">Situação</th></tr></thead><tbody class="divide-y divide-white/5">@foreach($data->take(12) as $row)<tr><td class="py-3 font-semibold">{{ $row->name }}</td><td class="py-3">{{ \App\Support\DecimalFormatter::format((string) $row->balance, 0) }} {{ $row->stock_unit }}</td><td class="py-3"><span class="rounded-full px-2 py-1 text-xs font-bold {{ $row->situation === 'critical' ? 'bg-red-500/15 text-red-300' : ($row->situation === 'attention' ? 'bg-amber-400/15 text-amber-300' : 'bg-emerald-500/15 text-emerald-300') }}">{{ ['critical'=>'Crítico','attention'=>'Atenção','ok'=>'OK','not_configured'=>'Sem meta'][$row->situation] }}</span></td></tr>@endforeach</tbody></table></div>@endif
</section>
