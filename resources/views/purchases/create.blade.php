@extends('layouts.app')
@section('title','Novo documento')
@section('content')
<div class="page-header"><h1 class="page-title">Novo documento de fornecedor</h1></div><form class="form-card max-w-4xl" method="POST" action="{{ route('purchases.store') }}">@csrf<input type="hidden" name="idempotency_key" value="{{ $key }}"><div class="grid gap-4 sm:grid-cols-2"><label><span class="form-label">Tipo</span><select class="form-input" name="document_type"><option value="invoice">Nota fiscal</option><option value="boleto">Boleto</option><option value="bill">Fatura</option><option value="receipt">Recibo</option><option value="proof">Comprovante</option><option value="other">Outro</option></select></label><label><span class="form-label">Número</span><input class="form-input" name="document_number"></label><label><span class="form-label">Fornecedor</span><select class="form-input" name="supplier_id"><option value="">Não identificado</option>@foreach($suppliers as $s)<option value="{{ $s->id }}">{{ $s->name }}</option>@endforeach</select></label><label><span class="form-label">Unidade</span><select class="form-input" name="location_id">@foreach($locations as $l)<option value="{{ $l->id }}">{{ $l->name }}</option>@endforeach</select></label><label><span class="form-label">Emissão</span><input class="form-input" type="date" name="issue_date" required></label><label><span class="form-label">Vencimento</span><input class="form-input" type="date" name="due_date"></label><label><span class="form-label">Valor total</span><input class="form-input" name="total_amount" required></label></div><p class="mt-5 text-sm text-stone-500">Itens e vínculos com insumos são opcionais; nenhum cadastro ou preço será criado automaticamente.</p><button class="btn-primary mt-5">Salvar documento</button></form>
@endsection
