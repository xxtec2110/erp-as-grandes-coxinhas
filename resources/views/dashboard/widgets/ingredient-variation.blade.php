<section class="rounded-2xl border border-white/10 bg-white/[0.04] p-5 sm:p-6"><div><p class="text-xs font-black uppercase tracking-[0.16em] text-amber-400">Custos sensíveis</p><h2 class="mt-1 text-xl font-black">{{ $widget['name'] }}</h2></div>
@if($data->isEmpty())<p class="mt-5 text-sm text-stone-400">Sem histórico suficiente no período.</p>@else<div class="mt-5 overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="text-xs uppercase text-stone-500"><tr><th class="pb-3">Insumo</th><th class="pb-3">Preço atual/base</th><th class="pb-3">Variação</th><th class="pb-3">Tendência</th></tr></thead><tbody class="divide-y divide-white/5">@foreach($data as $row)@php($variation=$row['variation_percentage'])<tr><td class="py-3 font-semibold">{{ $row['ingredient']->name }}</td><td class="py-3">R$ {{ \App\Support\DecimalFormatter::format((string) $row['final']->base_unit_cost, 6) }}</td><td class="py-3">{{ $variation === null ? 'Neutra' : \App\Support\DecimalFormatter::format((string) $variation, 2).'%' }}</td><td class="py-3 {{ $variation !== null && (float)$variation > 0 ? 'text-red-300' : (($variation !== null && (float)$variation < 0) ? 'text-emerald-300' : 'text-stone-400') }}">{{ $variation === null || (float)$variation === 0.0 ? 'Estável' : ((float)$variation > 0 ? 'Alta' : 'Baixa') }}</td></tr>@endforeach</tbody></table></div>@endif</section>
