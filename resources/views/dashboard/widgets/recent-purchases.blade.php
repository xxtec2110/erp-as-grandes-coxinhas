<section class="h-full rounded-2xl border border-white/10 bg-white/[0.04] p-5"><p class="text-xs font-black uppercase tracking-[0.16em] text-amber-400">Compras</p><h2 class="mt-1 text-lg font-black">{{ $widget['name'] }}</h2>@if($data->isEmpty())<p class="mt-5 text-sm text-stone-400">Nenhuma compra confirmada encontrada.</p>@else<ul class="mt-5 space-y-3">@foreach($data as $row)<li class="flex items-start justify-between gap-3 border-b border-white/5 pb-3"><div class="min-w-0"><p class="truncate font-semibold">{{ $row->supplier?->name ?? 'Fornecedor não informado' }}</p><p class="text-xs text-stone-400">{{ $row->document_type }} {{ $row->document_number }} · {{ $row->issue_date->format('d/m/Y') }}</p></div><strong class="whitespace-nowrap text-sm">R$ {{ \App\Support\DecimalFormatter::format((string) $row->total_amount, 2) }}</strong></li>@endforeach</ul>@endif</section>
