@extends('layouts.app')
@section('title','Estoque de insumos')
@section('content')
<div class="page-header"><div><h1 class="page-title">Estoque de insumos</h1><p class="page-subtitle">Saldo derivado exclusivamente dos movimentos oficiais.</p></div></div><form class="form-card mb-6 grid gap-3 sm:grid-cols-4"><select class="form-input" name="location_id">@foreach($locations as $item)<option value="{{ $item->id }}" @selected($location?->id===$item->id)>{{ $item->name }}</option>@endforeach</select><select class="form-input" name="category_id"><option value="">Todas as categorias</option>@foreach($categories as $category)<option value="{{ $category->id }}">{{ $category->name }}</option>@endforeach</select><input class="form-input" name="ingredient" placeholder="Buscar insumo" value="{{ request('ingredient') }}"><button class="btn-primary">Filtrar</button></form>
<div class="table-card"><table class="data-table"><thead><tr><th>Insumo</th><th>Saldo</th><th>Último movimento</th><th></th></tr></thead><tbody>@foreach($rows as $row)<tr><td>{{ $row['ingredient']->name }}</td><td>{{ \App\Support\DecimalFormatter::format($row['balance'],3) }} {{ $row['ingredient']->base_unit }}</td><td>{{ $row['last_movement']?->type ?? '—' }}</td><td><a class="text-link" href="{{ route('ingredient-stock.show',[$row['ingredient'],'location_id'=>$location->id]) }}">Histórico</a></td></tr>@endforeach</tbody></table></div>
<div class="mt-6 grid gap-5 lg:grid-cols-2">@foreach([['Perda de insumo','losses'],['Ajuste controlado','adjustments']] as [$title,$route])<form class="form-card" method="POST" action="{{ route('ingredient-stock.'.$route.'.store') }}">@csrf<input type="hidden" name="idempotency_key" value="{{ \Illuminate\Support\Str::uuid() }}"><h2 class="text-lg font-bold">{{ $title }}</h2><div class="mt-3 grid gap-3 sm:grid-cols-2"><select class="form-input" name="ingredient_id">@foreach($ingredients as $ingredient)<option value="{{ $ingredient->id }}">{{ $ingredient->name }}</option>@endforeach</select><select class="form-input" name="location_id">@foreach($locations as $item)<option value="{{ $item->id }}">{{ $item->name }}</option>@endforeach</select><input class="form-input" name="quantity" placeholder="Quantidade" required><select class="form-input" name="unit"><option>g</option><option>kg</option><option>ml</option><option>l</option><option>un</option></select>@if($route==='adjustments')<select class="form-input" name="direction"><option value="positive">Entrada</option><option value="negative">Saída</option></select>@endif<input class="form-input" type="date" name="operation_date" value="{{ now()->toDateString() }}"><input class="form-input sm:col-span-2" name="reason" placeholder="Motivo obrigatório" required></div><button class="btn-secondary mt-4">Registrar</button></form>@endforeach</div>
@endsection
