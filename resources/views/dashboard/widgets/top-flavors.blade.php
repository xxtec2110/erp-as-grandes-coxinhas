<section class="h-full rounded-2xl border border-white/10 bg-white/[0.04] p-5"><p class="text-xs font-black uppercase tracking-[0.16em] text-amber-400">Ranking</p><h2 class="mt-1 text-lg font-black">{{ $widget['name'] }}</h2>
@if($data->isEmpty())<p class="mt-5 text-sm text-stone-400">Sem dados de vendas no período.</p>@else<ol class="mt-5 space-y-3">@foreach($data as $row)<li class="flex items-center gap-3"><span class="grid h-7 w-7 place-items-center rounded-lg bg-amber-400/15 text-xs font-black text-amber-300">{{ $loop->iteration }}</span><span class="min-w-0 flex-1 truncate font-semibold">{{ $row->name }}</span><span class="text-sm font-black">{{ \App\Support\DecimalFormatter::format((string) $row->quantity, 0) }}</span></li>@endforeach</ol>@endif</section>
