<section class="rounded-2xl border border-white/10 bg-white/[0.04] p-5 sm:p-6"><div><p class="text-xs font-black uppercase tracking-[0.16em] text-amber-400">Custos & margens</p><h2 class="mt-1 text-xl font-black">{{ $widget['name'] }}</h2></div>
    @if($data->isEmpty())<p class="mt-5 text-sm text-stone-400">Nenhum produto cadastrado.</p>@else<div class="mt-5 overflow-x-auto"><table class="min-w-[760px] w-full text-left text-sm"><thead class="text-xs uppercase text-stone-500"><tr><th class="pb-3">Sabor</th><th class="pb-3">Preço</th><th class="pb-3">Custo atual</th><th class="pb-3">Lucro unitário</th><th class="pb-3">Margem</th></tr></thead><tbody class="divide-y divide-white/5">@foreach($data as $row)<tr><td class="py-3 font-semibold">{{ $row['product']->name }}</td><td class="py-3">{{ $row['sellingPrice'] === null ? 'Sem preço' : 'R$ '.\App\Support\DecimalFormatter::format((string) $row['sellingPrice'], 2) }}</td>@if($row['unit_cost'] === null)<td colspan="3" class="py-3 text-amber-300">Sem custo calculado</td>@else<td class="py-3">R$ {{ \App\Support\DecimalFormatter::format((string) $row['unit_cost'], 4) }}</td><td class="py-3">{{ $row['gross_profit'] === null ? '—' : 'R$ '.\App\Support\DecimalFormatter::format((string) $row['gross_profit'], 2) }}</td><td class="py-3">{{ $row['gross_margin_percentage'] === null ? '—' : \App\Support\DecimalFormatter::format((string) $row['gross_margin_percentage'], 1).'%' }}</td>@endif</tr>@endforeach</tbody></table></div>@endif
</section>
